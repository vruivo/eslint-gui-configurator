{
  "name": "hello-eslint",
  "version": "1.0.0",
  "description": "simple app with eslint",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "(./node_modules/.bin/eslint . 2>/dev/null) || (eslint . 2>/dev/null)"
  },
  "keywords": [
    "eslint"
  ],
  "author": "Vitor Ruivo",
  "license": "ISC",
  "devDependencies": {
  }
}
